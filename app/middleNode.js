// const { metacall, metacall_load_from_file } = require("metacall");

// metacall_load_from_file("c", ["./leaf.c"]);

// function process_middle(node) {
//   const left = metacall("process_leaf", 2 * node);
//   const right = metacall("process_leaf", 2 * node + 1);
//   return `Middle(${node}) ` + left + right;
// }

// module.exports = { process_middle };
